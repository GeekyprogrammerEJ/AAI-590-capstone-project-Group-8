# Data pipeline + EDA dependencies (Module 3)
datasets>=2.18
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
pyarrow>=12.0        # parquet engine for the processed corpus

# From-scratch baseline model (Module 5)
torch>=2.1          # Transformer answer-selection baseline (CPU / MPS / CUDA)
